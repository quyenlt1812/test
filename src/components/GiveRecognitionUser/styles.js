import {StyleSheet} from 'react-native';
import colors from '../../constants/colors';

const styles = StyleSheet.create({
  contentContainer: {borderBottomWidth: 1, borderColor: colors.WHITESMOKE},
  fullFlex: {flex: 1},
});

export default styles;
