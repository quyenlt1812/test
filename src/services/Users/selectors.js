export const getCurrentUser = (state) => state.users.currentUser;

export const getPeers = (state) => state.users.peers;
export const getColleagues = (state) => state.users.colleagues;
export const getUserRewards = (state) => state.users.rewards;
